# OneClick Subtitle Generator - Production Dependencies
# Pinned versions for production use

# Core audio processing
librosa==0.10.1
soundfile==0.12.1

# PyTorch for ML models
torch==2.2.1
torchaudio==2.2.1

# Translation
deep-translator==1.11.4

# FFmpeg wrapper
ffmpeg-python==0.2.0

# Platform-specific Whisper backends (handled by setup.py)
# These are installed conditionally based on platform

[dev]
# Development dependencies
pytest==8.1.1
pytest-cov==4.1.0
black==24.3.0
flake8==7.0.0
mypy==1.8.0
pylint==3.0.3

[test]
# Test dependencies
pytest==8.1.1
pytest-cov==4.1.0
pytest-mock==3.12.0

[doc]
# Documentation dependencies
sphinx==7.2.6
sphinx-rtd-theme==2.0.0

[all]
# All optional dependencies (install with: pip install -e .[all])
googletrans==4.0.0-rc1